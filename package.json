{
  "name": "catan-dice",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix webapp install && npm --prefix webapp run dev",
    "build": "npm --prefix webapp install && npm --prefix webapp run build",
    "serve": "npm run build && node server.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
